using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// TODO 산 타일들 위부터 order in layer 설정
public class Mountain : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
